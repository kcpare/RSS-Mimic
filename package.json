{
  "name": "rss-mimic",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "setup" : "npx playwright test --project=setup",
    "setup-no-deps" : "npx playwright test --project=setup --no-deps",
    "teardown" : "npx playwright test --project=teardown",
    "firefox" : "npx playwright test --project=firefox",
    "firefox-no-deps" : "npx playwright test --project=firefox --no-deps",
    "chrome" : "npx playwright test --project=chromium",
    "chrome-no-deps" : "npx playwright test --project=chromium --no-deps",
    "safari" : "npx playwright test --project=webkit",
    "safari-no-deps" : "npx playwright test --project=webkit --no-deps"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.49.1",
    "@types/node": "^22.10.4"
  },
  "dependencies": {
    "dotenv": "^16.4.7"
  }
}
